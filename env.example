# Database Configuration
SPRING_DATASOURCE_URL=jdbc:postgresql://auth-db:5432/authdb
SPRING_DATASOURCE_USERNAME=user
SPRING_DATASOURCE_PASSWORD=password
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# RabbitMQ Configuration
RABBITMQ_HOST=rabbitmq
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest

# JWT Configuration
# En az 32 byte (256 bit) bir secret kullanın
JWT_SECRET=bezKoderSecretKeyMustBeAtLeast32Bytes!!
JWT_EXPIRATION_MS=86400000

# PostgreSQL Database Configuration
POSTGRES_DB=authdb
POSTGRES_USER=user
POSTGRES_PASSWORD=password

# Email Service Database Configuration
EMAIL_DB_NAME=emaildb
EMAIL_DB_USER=emailuser
EMAIL_DB_PASSWORD=emailpassword

# Mailhog/SMTP (development için)
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_SMTP_AUTH=false
MAIL_SMTP_STARTTLS=false
